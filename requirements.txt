# Telegram Parser & Inviter PRO v5.0 — зависимости
# ✦ crafted by zippa ✦
#
# Базовые (обязательные):
telethon>=1.34.0
cryptography>=42.0
pysocks>=1.7.1

# Для сборки в EXE (опционально):
pyinstaller>=6.0

# Примечание: pycryptodome НЕ нужен — AES-IGE для TData реализован
# на чистом Python через cryptography.hazmat.primitives.ciphers
#
# Tkinter входит в стандартную поставку Python.
# На некоторых Linux-дистрибутивах может потребоваться:
#   sudo apt install python3-tk        (Ubuntu/Debian)
#   sudo dnf install python3-tkinter   (Fedora)
